import * as React from 'react';
import './glitchAnimation.css';

const AnimateGlitch: React.FC = () => {
  // This component simply imports the CSS and doesn't render anything
  return null;
};

export default React.memo(AnimateGlitch);